[install]
# cooldown: don't resolve versions published in the last 3 days
minimumReleaseAge = 259200
minimumReleaseAgeExcludes = []   # keep empty unless you have a real reason

frozenLockfile = false           # true in CI via `bun ci` instead
linker = "isolated"              # no phantom deps